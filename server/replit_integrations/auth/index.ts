export { setupAuth, isAuthenticated, getSession, hashPassword, comparePasswords } from "./replitAuth";
export { authStorage, type IAuthStorage } from "./storage";
export { registerAuthRoutes } from "./routes";
